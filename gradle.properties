# Gradle daemon settings. Deliberately no `org.gradle.java.home` here — that
# pins an absolute JDK path from one developer's machine and breaks every
# other one. The Java version is declared by the toolchain in build.gradle.kts
# (Java 17), which Gradle resolves from whatever JDKs are installed locally.
org.gradle.jvmargs=-Xmx2g
